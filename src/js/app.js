// Console log Welcome
console.log('Gulp dev server is running');

// Seeing if we are connected to local host
if (location.hostname === 'localhost' || location.hostname === '127.0.0.1') {
  // Pulling element
  const colorBar = document.getElementById('color-bar');

  // Adding animayion class
  //   colorBar.classList.add('color-bar-animation');
}

const lastBox = document.getElementById('color-bar');

console.log(lastBox);
